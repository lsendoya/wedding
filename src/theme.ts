import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    body: 'PearlFont, system-ui, sans-serif',
    heading: 'Pearl, Georgia, serif',
  },
});

export default theme;
